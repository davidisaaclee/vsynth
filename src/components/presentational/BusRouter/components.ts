import styled from '../../../styled-components';
import ParameterControl from '../ParameterControl';

export const LaneRow = styled.tr`
	height: 30px;
`;

export const LaneHeader = styled.th`
	position: relative;

	padding: 5px;
	width: 80px;

	text-align: right;
`;

export const StyledParameterControl = styled(ParameterControl)`
	height: 20px;
`;

